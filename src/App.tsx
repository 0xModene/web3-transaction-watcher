import TransactionWatcher from "lib/components/TransactionWatcher";
import useTransactionWatcher from "lib/hooks/useTransactionWatcher";
import React from "react";

const App: React.FC = () => {
  return <TransactionWatcher></TransactionWatcher>;
};

export default App;
